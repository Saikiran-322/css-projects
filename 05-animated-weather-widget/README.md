☀️ Project: Animated Weather Widget

Build an animated UI card that represents weather using CSS transitions & keyframes.

🧠 Core Concepts Practiced

1. CSS Transitions

Smooth change on hover or click

transition: all 0.3s ease;

2. CSS Keyframes Animation

You define custom animation steps:

@keyframes glow {
0% { opacity: 0.7; }
100% { opacity: 1; }
}

3. Animation Properties

animation-name

animation-duration

animation-timing-function

animation-iteration-count

animation-direction

4. Hover Animations
   .card:hover { transform: scale(1.05); }
